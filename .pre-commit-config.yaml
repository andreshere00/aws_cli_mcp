repos:
  - repo: https://github.com/pycqa/isort
    rev: 6.0.1
    hooks:
      - id: isort

  - repo: https://github.com/psf/black
    rev: 24.3.0
    hooks:
      - id: black
        language_version: python3

  - repo: https://github.com/pycqa/flake8
    rev: 7.0.0
    hooks:
      - id: flake8
        args: [--max-line-length=100]
        language_version: python3

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-yaml
      - id: check-json

  # - repo: local
  #   hooks:
  #     - id: check-test-coverage
  #       name: Test coverage (>=75%)
  #       entry: |
  #         bash -c '
  #         export GENAI_ENVIRONMENT=test
  #         uv run coverage run -m pytest --junitxml=xunit-result.xml
  #         TOTAL=$(uv run coverage report | awk "/^TOTAL/ { gsub(/%/,\"\"); print \$4 }")
  #         echo "Total coverage: ${TOTAL}%"
  #         if (( $(echo "$TOTAL < 75" | bc -l) )); then
  #           echo "ERROR: Coverage ${TOTAL}% is below 75% threshold."
  #           exit 1
  #         fi
  #         '
  #       language: system
  #       pass_filenames: false
  #       always_run: true
  #       require_serial: true
